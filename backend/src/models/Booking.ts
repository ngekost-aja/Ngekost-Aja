export default interface Booking {
    id: number;
    userId: number;
    houseId: number;
    startDate: string;
    endDate: string;
    status: string;
    totalPrice: string;
    createdAt: string;
    updatedAt: string;
}
